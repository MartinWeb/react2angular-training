import { Alert } from "components/Alert/Alert";

export const Warning = () => (
    <Alert type="warning" title="Be very careful not to mock this website's design !">
        <p>Chuck loves orange and blue, because it's the colour of his beard and his favorite jeans.</p>
    </Alert>
);
